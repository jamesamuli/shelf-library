# assets

Non-code reference material used while migrating from legacy PMB: screenshots
of existing screens, sample data exports, icons/logos pulled from the legacy
app for parity checks.

Nothing in this folder is built or bundled by the app — for static files the
app actually serves, use `public/` instead.

No assets have been added yet; this folder is prepared ahead of the first
migration task.
